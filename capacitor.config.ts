import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isi85.demo0001',
  appName: 'gapapp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
